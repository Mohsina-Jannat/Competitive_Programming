#include<bits/stdc++.h>
using namespace std;
int main()
{
    int diff;
    scanf("%d",&diff);
    char str[107];
    scanf("%s",str);
    int len=strlen(str);
    vector<int> vt;
    for(int i=0;i<len;i++)
    {
        if(str[i]==)
    }

    return 0;
}
